# Shared / fallback (do not commit real secrets)
NODE_ENV=development
APP_ENV=dev

# PostgreSQL (local: docker compose up -d)
DATABASE_URL="postgresql://prosyvaisya:prosyvaisya@localhost:5432/prosyvaisya_dev?schema=public"

JWT_SECRET="change-me-dev-secret-min-32-chars-long!!"
JWT_EXPIRES_IN="7d"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_API_BASE_URL="http://localhost:3000/api/v1"
